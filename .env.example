# Supabase (waitlist storage)
# Create a table: waitlist (id uuid primary key default gen_random_uuid(), name text, email text unique, business_type text, monthly_budget text, created_at timestamptz default now())
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Resend (confirmation emails)
# Get API key at https://resend.com
RESEND_API_KEY=re_xxxx
RESEND_FROM_EMAIL=notifications@yourdomain.com

# Optional: Google Analytics (replace in app/layout.tsx)
# G-XXXXXXXXXX
